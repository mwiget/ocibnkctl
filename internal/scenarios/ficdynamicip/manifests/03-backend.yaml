apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-fic-index
  namespace: scn-fic-dyn
data:
  index.html: |
    ocibnkctl-scenario-fic-dynamic-ip-OK
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx
  namespace: scn-fic-dyn
  labels: {app: nginx-fic}
spec:
  replicas: 2
  selector:
    matchLabels: {app: nginx-fic}
  template:
    metadata:
      labels: {app: nginx-fic}
    spec:
      containers:
      - name: nginx
        image: nginx:1.27-alpine
        ports: [{containerPort: 80}]
        volumeMounts:
        - {name: html, mountPath: /usr/share/nginx/html}
        readinessProbe:
          httpGet: {path: /, port: 80}
          initialDelaySeconds: 1
          periodSeconds: 2
        resources:
          requests: {cpu: 10m, memory: 16Mi}
          limits:   {cpu: 100m, memory: 64Mi}
      volumes:
      - name: html
        configMap: {name: nginx-fic-index}
---
apiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: scn-fic-dyn
spec:
  selector: {app: nginx-fic}
  ports:
  - {name: http, port: 80, targetPort: 80}
